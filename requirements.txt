aiohttp==3.9.5
httpx==0.28.1
loguru==0.7.2
matplotlib>=3.8.0
numpy==1.26.4
pandas>=2.0.0
Pillow==10.3.0
psutil>=5.9.0
python-dotenv==1.0.1
pyyaml==6.0.1
openpyxl>=3.1.0
websockets>=12.0

# --- info-edge scanner (V0, см. SCANNER_SPEC.md) — keyless/CPU ---
trafilatura==2.0.0
requests==2.32.5
brotli
rapidfuzz==3.14.5     # event-дедуп заголовков (Этап 2)

# Test/dev tooling
pytest>=8.0
ruff>=0.6.0

# --- OPTIONAL GPU sweep backend (NOT required; the lab runs CPU without it) ---
# The momentum_breakout signal kernel can run on a real GPU via cupy. Install
# only to enable backend=gpu/auto. NVIDIA CUDA 12 wheels (bundle the toolkit):
#   pip install "cupy-cuda12x[ctk]"          # numpy 2.x line (cupy 14)
#   pip install "numpy>=1.26,<2" "cupy-cuda12x>=13,<14"   # keep numpy 1.26 pin
# Then verify: python -m scripts.strategy_lab.gpu_doctor
# cupy is intentionally absent here so a CPU-only install never pulls CUDA wheels.
