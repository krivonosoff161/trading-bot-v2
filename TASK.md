# TASK для Codex — шортлист keyless-источников для Scout #1 (31.05.2026)

## Цель
Из `cporter202/API-mega-list` собрать **шортлист 10-15 keyless/бесплатных крипто-релевантных источников** (новости/данные/события), которых у нас ЕЩЁ НЕТ, — кандидатов на добавление в Scout #1. Не интеграция кода, а курированный список с полями для проверки.

## Контекст (прочитай сначала)
- `ROADMAP.md` (направление), `docs/repo_catalog_2026-05-31.md` (нужность).
- **Что у нас УЖЕ есть** (НЕ дублировать): `scripts/analysis/research/daily_digest_collector.py` — источники CoinGecko (global/markets/trending), RSS (Cointelegraph/Decrypt/CoinDesk/BitcoinMagazine), Fear&Greed (alternative.me), OKX public; `onchain_whales_probe.py` — DeFiLlama (stablecoins/TVL/DEX), mempool.space, blockchain.info.
- `API-mega-list` — категории `News`, `MCP Servers`, `Open Source`, `Social Media`, `Data Collection`.

## Шаги
1. Просей релевантные категории; отбери **крипто/финанс-релевантные** источники новостей/данных/событий.
2. По КАЖДОМУ кандидату поля: `name` · `url` · категория · **keyless?** (без ключа) · **free tier?** · формат (RSS/JSON/API) · крипто-релевантность (high/med) · rate-limit (если известен) · **можно ли OOS-логировать** (отдаёт ли структурно: заголовок+время / число+время).
3. **Исключи** всё, что у нас уже есть (список выше) и всё, что требует платного ключа по умолчанию.
4. Отметь топ-5 «брать первыми» с одной строкой почему.

## Готово когда
- Создан `docs/scout1_source_candidates_2026-05-31.md`: таблица 10-15 кандидатов + блок «топ-5 первыми».
- Дописан блок в `SESSION.md`: «↪ Codex сделал: шортлист источников Scout #1 → топ-5: <...>, файл docs/scout1_source_candidates_2026-05-31.md» + коммит.

## Граница (строго)
- **READ-ONLY.** Не трогать `.env`/`AUTO_TRADE`/`config.yaml`/прод. Не добавлять ключи.
- **Не копировать таблицы/код из API-mega-list** (у репо нет лицензии) — только навигация: ссылки + наши поля.
- Не запускать чужой код. Источник «звучит keyless» ≠ «проверен» — финальную живую проверку сделает Claude, ты просто помечаешь заявленное.
- Не выдумывать источники/цифры; не уверен — пиши «не проверено».
