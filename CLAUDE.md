# CLAUDE.md — Инструкции для AI агентов

## Проект

Trading Bot V2 — OKX фьючерсный скальпинг бот.
Цель: стабильный доход на демо → реальный счёт → инвесторы.

**Текущий этап:** Фаза 1.1 — Фундамент.
**Активный план:** `PLAN.md`
**Видение:** `PROJECT_VISION.md`

---

## Язык

- Общение с пользователем: **только русский**
- Код, комментарии, логи: английский

---

## Главные правила

### ALWAYS
- Читать `PLAN.md` перед любой задачей — работаем только в рамках текущей фазы
- Простота прежде всего — минимум кода для текущей задачи
- Один файл изменён = один коммит
- Перед изменением кода — показать план, дождаться одобрения
- Логи должны быть читаемы: symbol, reason, price, side

### NEVER
- Не добавлять фичи не из текущей фазы
- Не брать код из старого проекта без явного запроса
- Не коммитить без одобрения пользователя
- Не хранить API ключи в коде или конфигах
- Не усложнять то что работает просто

---

## Структура проекта

```
trading-bot-v2/
├── CLAUDE.md           ← этот файл
├── PROJECT_VISION.md   ← цели и этапы
├── PLAN.md             ← активный план фаз
├── config.yaml         ← конфиг (без ключей!)
├── .env                ← API ключи (в .gitignore!)
├── src/
│   ├── exchange/       ← OKX клиент, WebSocket
│   ├── strategy/       ← торговая логика
│   └── utils/          ← логирование, хелперы
├── logs/               ← логи (в .gitignore)
└── tests/              ← тесты
```

---

## Команда ИИ и роли

| Агент | Роль |
|-------|------|
| Claude Code | Архитектура, код-ревью, анализ, основной |
| Kimi | Анализ больших логов (>10k строк) |
| Cursor | Автодополнение при написании кода |

---

## Старый проект

Путь: `c:\Users\krivo\simple trading bot okx\`
Статус: **только справочник, не изменять**

Что можно брать:
- OKX REST/WebSocket клиент
- OCO механизм
- Reversal detection логика

---

## Git стандарт

- Ветка: `main` — только стабильный код
- Фича ветки: `feature/название`
- Коммит: `тип: краткое описание` (feat/fix/docs/config)
- Пуш только после теста

---

## Конфиг

Два файла:
- `config.yaml` — параметры стратегии (коммитится)
- `.env` — API ключи (НИКОГДА не коммитить)
