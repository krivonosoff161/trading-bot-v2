# GPT — Шлифовка трёх движков, ПОЛНЫЙ перебор (22.05.2026)

## Контекст

Прогон 3-движков показал: разделение верное, но у каждого свой блокер:
- **импульс** — НЕ оценён: дыра в тиках на волатильных альтах в окне 04-14.05 (всего 2 fill).
- **тренд** — ride РАБОТАЕТ (trend_01: +18%, capture 59%), но НАПРАВЛЕНИЕ сломано (dir match 35% — хуже монетки).
- **фейд** — ближе всех (net −0.10%, dir 82%, edge 67%), но цель на дешёвых монетах кривая + capture низкий (36%).

**Задача:** отшлифовать КАЖДЫЙ движок ПОЛНЫМ перебором, найти где прибыль, а где мы лоси — честно.
Можно гонять **долго (4-5 часов)** — лучше тщательно, чем быстро. Не тормозить, перебирать, самому
смотреть на данные и находить где упустили вход.

**Стратегический контекст (важно):** прод БУДЕТ переделан под результаты — НЕ ограничивай себя текущей
прод-логикой. Сейчас этап сбора данных и поиска эджа. Главное — **tape-данные** (на них всё строится).

---

## ⚠️ БЕЗОПАСНОСТЬ
- Только `scripts/analysis/research/` и `output/`, суффикс `_polish_22_05_2026`.
- Импорт `compute_signal` / переиспользование прошлых харнессов / tape-загрузчика — на чтение.
  Прод (`src/`, `config.yaml`, `scripts/ws/`, `main.py`, `logs/`) НЕ трогать вообще.
- Бриф из `docs/`, не копировать в research/.

## Данные (КЛЮЧЕВОЕ)
- Грузить ВСЁ: свечи (4H/1H/15m/5m/1m, догружать из OKX REST) + **tape тики** `E:\trading-data\ticks`.
- **Проверить tape-покрытие ПО ПАРАМ и по датам** — и для каждого движка выбрать период с нормальным
  покрытием (см. импульс ниже). Логировать coverage. Никаких candle-прокси под видом тиков.

---

## ДВИЖОК 1 — ИМПУЛЬС (ПРИОРИТЕТ: сначала почини данные)
- **СНАЧАЛА:** проверь tape-покрытие; выбери период, где у волатильных альтов (BSB/EDEN/RLS/CHZ/SPACE/
  NOT/TURBO/BOME) ЕСТЬ тики (вероятно ~12-22.05), и гони импульс НА НЁМ — не на 04-14, где их нет.
- **Перебор early-trigger окон:** 10s / 20s / 60s / 2min / 5min — показать ГРАДИЕНТ: чем раньше вход,
  тем выше capture? Tick-only, без подмены.
- Перебор структурного выхода (k1/k2/k3, giveback 30/40/50).
- Перебор порога импульса (тело ≥ N× среднего) + объём.
- Метрики: capture, **entry lag**, net по сторонам (short был сильнее — проверить), по тирам волатильности.

## ДВИЖОК 2 — ТРЕНД (направление сломано)
- Ride подтверждён (trend_01 capture 59%). Чинить НАПРАВЛЕНИЕ (dir match 35%).
- **Взять направление от РЕАЛЬНОГО движка** (`compute_signal` side / логика, что даёт продовый DRIFT×FAST
  WR 91%), а НЕ от кривого структурного реимпла. Показать dir match: старый (35%) vs новый.
- Перебор ride-выходов: структурный трейл (k), широкий ATR-трейл, выход на сломе тренда (EMA/структура).
- Бенчмарк — продовый DRIFT×FAST. Метрики: % устойчивого хода пойман, hold (часы), net по сторонам/тирам.

## ДВИЖОК 3 — ФЕЙД (дошлифовать — ближе всех к рабочему)
- **Почини расчёт цели/стопа на ДЕШЁВЫХ монетах** (fade_01 PEPE: уровни схлопнулись ≈4.00 при цене
  4.3e-6 — как precision-bug). Округление по tick size / значащим цифрам, не фикс-знаки.
- Перебор: толерантность касания границы BB, цель (BB middle vs противоположная граница vs giveback),
  условия активации (ADX, ширина коридора, day-position).
- Цель — поднять capture (сейчас 36%) и флипнуть net в плюс.
- Метрики: fade WR, R до средней, касаний/день, по сторонам/тирам.

---

## Общее по каждому движку
- СВОЙ перебор + СВОЯ таблица метрик + `edge_exists` vs `capture`.
- **6-8 картинок на движок** с разметкой: вход / выход / что упустили (доступный ход vs пойманный).
- GO/NO-GO строгий: net+ после `0.20%` тейкер + slippage, обе стороны (где применимо), держится
  рано/поздно, n≥20. Тонкие/односторонние плюсы — research-кандидаты, не «годно».

## Свобода (GPT)
- Итерируй, перебирай, смотри на данные сам. Где упустили вход — найди и покажи.
- Блок «Гипотезы GPT» по каждому движку: где прибыль, где edge есть но не ловим, где edge нет.
- Если движок нашими данными не торгуется — скажи прямо. Это проверка, не подгонка.

## Стандарты кода
PEP8, английские комментарии/логи, параметры конфиг-словарём, импорт прод-логики только на чтение.

## Файлы (суффикс `_polish_22_05_2026`, только research/)
1. `.py` — три движка с переборами + роутер + рендер.
2. Отчёт `.md`: ТРИ раздела, у каждого sweep-таблица + своя метрика + вывод + условия детекции.
3. Картинки по каждому движку.
4. Coverage-отчёт (что загружено, какой период по каждому движку). Большой сырой json НЕ коммитить.
